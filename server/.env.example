# Local dev
# Uses the Postgres started via ../docker-compose.yml
DATABASE_URL=postgresql://vasteroids:vasteroids@localhost:5432/vasteroids

# Comma-separated origins, or * to allow all
# Example for VS Code Live Server:
# CORS_ORIGIN=http://localhost:5500,http://127.0.0.1:5500
CORS_ORIGIN=*

# Optional: tighten rate limit for POST /api/scores
SCORE_POST_MAX_PER_MIN=60
